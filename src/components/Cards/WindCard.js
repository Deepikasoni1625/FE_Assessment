import React from "react";

export const WindCard = ()=>{
    return(
        <div style={styles.container}>

        </div>
    );
};

const styles = {
    container:{
        display:"flex",
        height:"42%",
        width:"30%",
        backgroundColor: "white",
        borderRadius: '1.5rem',
        marginBottom: '5%',
    }
}